#include<stdio.h>

int main() {
    //Taking single word string input.
   char word[10];
   printf("Enter string without spaces:");
   scanf("%s", word);
   printf("The string is %s\n", word);
   return 0;
}